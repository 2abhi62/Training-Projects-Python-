#include<stdio.h>
int main()
{
 int n,f;
printf("\n Enter a number");
 scanf("%d",&n);
 f =factorial(n);
 pritnf("\n The factorial of %d is %d=",n,f);
 return 0;
}
int factorial(int a)
{
 if(a==0||a==1)
 return 1;
 else
 return a*(factorial(a-1));
}
